# Operator Runbook — MOVED

This file has been retired. All runbook content (OAuth reconnect, scope errors,
trash-window restore, log locations, credential rotation) is now in
**[INSTALL.md — Operations section](../INSTALL.md#4-operations)** at the project root.
